# AUDIT FLOWIN v28 — 13/05/2026

## A. ARCHITECTURE : ÉCARTS CODE ↔ SQL

### A1. Table `events`
- ❌ Code envoie `e.proId` (camelCase) → SQL attend `pro_id` (OK, mapping fait dans supaWriteEvent)
- ❌ Code n'envoie PAS `client_type` → schema l'attend (colonne avec default OK)
- ✅ `cfg`, `stats`, `pro_visib` JSONB OK

### A2. Table `pros`
- ❌ Pas de `supaWritePro()` — la fonction n'existe pas
- ❌ `newPro()` n'écrit que en localStorage, jamais en Supabase
- ❌ Champs dashboard : `entreeP` (date) → schema = `entree_p` (OK si fonction existait)
- ❌ Schema manque : `role_contact` (existe en SQL), `tags TEXT[]` (existe en SQL)

### A3. Table `partenaires`
- ❌ Pas de `supaWritePartenaire()`
- ❌ Code dashboard utilise `type` (Local/National) → schema utilise `secteur`
- ❌ Code dashboard utilise `siret`, `contrat`, `role`, `events[]` → schema ne les a PAS
- ❌ Schema manque : `type`, `siret`, `contrat`, `role`, `events TEXT[]`

### A4. Table `banques`
- ❌ Pas de `supaWriteBanque()`
- ❌ `wizCreateBanque()` n'écrit qu'en localStorage

### A5. Table `super_events`
- ❌ Pas de `supaWriteSuperEvent()`
- Schema OK : `pros TEXT[]`, `events TEXT[]`, dates

### A6. Table `joueurs`
- ✅ `supaWriteJoueur()` existe et fonctionne
- ⚠️ Schema utilise `client_type` mais code envoie peut-être `clientType` (à vérifier)

### A7. Table `lots`
- ✅ `supaWriteLot()` existe
- Schema OK

### A8. Table `participations`
- ✅ Schema OK
- ❌ Aucun code dashboard ne lit cette table pour KPIs realtime

---

## B. BUGS UX/UI IDENTIFIÉS

### B1. wizSave (création event)
- ❌ Ne dual-write pas vers Supabase
- ❌ Si proId vide, l'event est créé sans pro → orphelin

### B2. newPro / newPartenaire / wizCreateBanque
- ❌ Pas de validation des champs obligatoires
- ❌ Pas de dual-write Supabase

### B3. modalReglagesSection (Paramètres)
- ⚠️ Template literals dans const, valeurs Supabase recalculées à chaque appel (OK)
- ❌ `renderParametres` corrigé avec return html (DÉJÀ FIXÉ)

### B4. Section Jeux
- ✅ Fix DATA.banques appliqué (vérifier déploiement Vercel)

### B5. Parcours joueur Supabase
- ✅ Dual-write Supabase ajouté (Pâques + NDS)
- ❌ Anti-doublon localStorage seulement — pas de vérif côté Supabase

### B6. QR codes events
- ❌ `qrUrl` dans cfg.qrUrl pointe vers ancienne URL Netlify
- ❌ Aucun mécanisme pour générer auto `https://flowin-events.vercel.app/parcours/...?ev=ID`

### B7. Tirage gagnants
- ❌ Modal tirage n'écrit pas en Supabase les lots assignés (le `assigne_a` reste local)

### B8. Stats live
- ❌ Pas de polling Supabase pour les KPIs participants/optin
- ❌ Le toggle "Realtime" dans Paramètres est purement UI, pas connecté

---

## C. MANQUES FONCTIONNELS

### C1. Création super event
- ❌ Manque dual-write
- ❌ Le champ "pros[]" et "events[]" ne sont pas synchronisés bidirectionnellement

### C2. Lien Event ↔ Partenaire ↔ Lot
- ❌ Quand un lot est créé avec `partenaireId`, le partenaire ne se voit pas mis à jour automatiquement avec l'event
- ❌ `lot.eventId` peut pointer vers un event qui n'existe plus (pas de FK check côté JS)

### C3. Vote module
- ❌ Pas de table `votes` en Supabase pour stocker les votes des participants
- ❌ Le module vote dans le parcours n'écrit rien en base

### C4. Spin module
- ❌ `cfg.spinSegments` stocké en JSONB OK
- ❌ Mais le tirage spin n'a pas de table de logs (qui a gagné quoi, quand)

